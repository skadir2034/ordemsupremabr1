import React from 'react';

export function GloriaParticlesBg() {
  // Removido completamente todo o papel de parede, meteoros e artes de fundo a pedido do usuário.
  // Retorna null para otimização absoluta (0% de uso de CPU/GPU).
  return null;
}
